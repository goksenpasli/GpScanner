<Window x:Class="GpScanner.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Extensions="clr-namespace:Extensions;assembly=Extensions"
    xmlns:ViewModel="clr-namespace:GpScanner.ViewModel" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:twainctrl="clr-namespace:TwainControl;assembly=TwainControl"
    Title="Tarayıcı"
    d:DataContext="{d:DesignInstance Type=ViewModel:GpScannerViewModel}"
    d:Height="600" d:Width="1280" UseLayoutRounding="True"
    WindowStartupLocation="CenterScreen" WindowState="Maximized" mc:Ignorable="d">
    <Window.Resources>
        <CollectionViewSource x:Key="Veriler" Source="{Binding Dosyalar}">
            <CollectionViewSource.SortDescriptions />
        </CollectionViewSource>
        <Extensions:SystemFileIconConverter x:Key="SystemFileIconConverter" />
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <twainctrl:TwainCtrl x:Name="TwainCtrl" />
        <GroupBox Grid.Column="1" Header="DOSYA">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="300" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Viewbox Stretch="Fill">
                    <Calendar DisplayDateEnd="{Binding Mode=OneWay, Source={x:Static sys:DateTime.Today}}"
                        PreviewMouseUp="Calendar_PreviewMouseUp"
                        SelectedDate="{Binding SeçiliGün}" />
                </Viewbox>
                <UniformGrid Grid.Row="1" Columns="2">
                    <Button Command="{Binding ResetFilter}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <Image Width="24" Height="24">
                                    <Image.Source>
                                        <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                                            <Binding Source="Shell32.dll" />
                                            <Binding Source="238" />
                                        </MultiBinding>
                                    </Image.Source>
                                </Image>
                                <TextBlock VerticalAlignment="Center" Text="TÜMÜNÜ GÖR" />
                            </StackPanel>
                        </Button.Content>
                    </Button>
                    <ToggleButton IsChecked="{Binding Path=ShowPdfPreview}">
                        <ToggleButton.Content>
                            <StackPanel Orientation="Horizontal">
                                <Image Width="24" Height="24">
                                    <Image.Source>
                                        <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                                            <Binding Source="Shell32.dll" />
                                            <Binding Source="1" />
                                        </MultiBinding>
                                    </Image.Source>
                                </Image>
                                <TextBlock VerticalAlignment="Center" Text="PDF DOSYA ÖNİZLEME" />
                            </StackPanel>
                        </ToggleButton.Content>
                    </ToggleButton>
                </UniformGrid>
                <TextBox Grid.Row="2" Text="{Binding AramaMetni, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <ListBox Grid.Row="3"
                    d:ItemsSource="{d:SampleData ItemCount=5}"
                    IsSynchronizedWithCurrentItem="False"
                    ItemsSource="{Binding Mode=OneWay, Source={StaticResource Veriler}}"
                    VirtualizingPanel.CacheLength="2,2" VirtualizingPanel.CacheLengthUnit="Item"
                    VirtualizingPanel.IsVirtualizingWhenGrouping="True" VirtualizingPanel.ScrollUnit="Pixel"
                    VirtualizingPanel.VirtualizationMode="Recycling">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <CheckBox VerticalAlignment="Center" VerticalContentAlignment="Center"
                                                IsChecked="{Binding Seçili}"
                                                IsEnabled="{Binding FileName, Mode=OneWay, Converter={StaticResource FileNamePdfCheckConverter}}">
                                                <CheckBox.Style>
                                                    <Style TargetType="CheckBox">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Path=DataContext.ShowPdfPreview, RelativeSource={RelativeSource AncestorType=Window}}" Value="true">
                                                                <Setter Property="Content">
                                                                    <Setter.Value>
                                                                        <ContentControl Width="84" Height="117"
                                                                            Content="{Binding Path=FileName}">
                                                                            <ContentControl.ContentTemplate>
                                                                                <DataTemplate>
                                                                                    <ViewModel:PdfViewer FirstPageThumbnail="True"
                                                                                        PdfFilePath="{Binding DataContext, Converter={StaticResource FilePathMergeConverter}, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                                                                        Template="{StaticResource PdfViewerControlTemplate}"
                                                                                        ToolBarVisibility="Collapsed" />
                                                                                </DataTemplate>
                                                                            </ContentControl.ContentTemplate>
                                                                        </ContentControl>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </CheckBox.Style>
                                            </CheckBox>
                                            <Extensions:SplitButton Command="{Binding DataContext.WebAdreseGit, ElementName=TwainCtrl, Mode=OneWay}"
                                                CommandParameter="{Binding Path=FileName}"
                                                SplitContentPartIsEnabled="{Binding FileName, Mode=OneWay, Converter={StaticResource FileNamePdfCheckConverter}}">
                                                <Extensions:SplitButton.Content>
                                                    <StackPanel Orientation="Horizontal">
                                                        <Image Width="32" Height="32"
                                                            Source="{Binding FileName, Converter={StaticResource FilePathToIconConverter}}" />
                                                        <TextBlock VerticalAlignment="Center" Text="{Binding Mode=OneWay, Path=FileName, Converter={StaticResource FileNameExtractConverter}}" />
                                                    </StackPanel>
                                                </Extensions:SplitButton.Content>
                                                <Extensions:SplitButton.InternalContent>
                                                    <Extensions:ResizablePanel Width="480" Height="360" MinWidth="480"
                                                        MinHeight="360">
                                                        <GroupBox>
                                                            <ContentControl Content="{Binding Path=FileName}" ContentTemplate="{StaticResource PdfViewerTemplate}" />
                                                        </GroupBox>
                                                    </Extensions:ResizablePanel>
                                                </Extensions:SplitButton.InternalContent>
                                            </Extensions:SplitButton>
                                            <TextBlock VerticalAlignment="Center">
                                                <Hyperlink Command="{Binding DataContext.ExploreFile, ElementName=TwainCtrl, Mode=OneWay}"
                                                    CommandParameter="{Binding Path=FileName}"
                                                    TextDecorations="None">
                                                    <Image Width="32" Height="32">
                                                        <Image.Source>
                                                            <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                                                                <Binding Source="Shell32.dll" />
                                                                <Binding Source="4" />
                                                            </MultiBinding>
                                                        </Image.Source>
                                                    </Image>
                                                </Hyperlink>
                                            </TextBlock>
                                        </StackPanel>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
                <Button Grid.Row="4" Command="{Binding PdfBirleştir}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <Image Width="24" Height="24">
                                <Image.Source>
                                    <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                                        <Binding Source="Shell32.dll" />
                                        <Binding Source="68" />
                                    </MultiBinding>
                                </Image.Source>
                            </Image>
                            <TextBlock VerticalAlignment="Center" Text="İŞARETLİ PDFLERİ BİRLEŞTİR" />
                        </StackPanel>
                    </Button.Content>
                </Button>
            </Grid>
        </GroupBox>
    </Grid>
</Window>