<UserControl x:Class="TwainControl.PdfImportViewerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Extensions="clr-namespace:Extensions;assembly=Extensions"
    xmlns:Pdfviewer="clr-namespace:PdfViewer;assembly=PdfViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TwainControl"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:properties="clr-namespace:TwainControl.Properties"
    d:DataContext="{d:DesignInstance Type=local:TwainCtrl}"
    d:DesignHeight="600" d:DesignWidth="420"
    local:Translation.ResourceManager="{x:Static properties:Resources.ResourceManager}"
    UseLayoutRounding="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Extensions:FilePathToIconConverter x:Key="FilePathToIconConverter" />
        <Extensions:SystemFileIconConverter x:Key="SystemFileIconConverter" />
        <Extensions:CloneConverter x:Key="CloneConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Margin="2" Orientation="Horizontal">
            <Button Command="{Binding LoadSingleEypFile}" CommandParameter="{Binding ElementName=PdfViewer, Mode=OneWay}">
                <StackPanel Orientation="Horizontal">
                    <Image Width="16" Height="16"
                        Source="{Binding Source='.eyp', Converter={StaticResource FilePathToIconConverter}}" />
                    <TextBlock VerticalAlignment="Center">
                        <Run Text="EYP" />
                        <Run Text="{local:Loc FILE}" />
                    </TextBlock>
                </StackPanel>
            </Button>
            <Grid DataContext="{Binding Path=DataContext, RelativeSource={RelativeSource AncestorType=Window}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Extensions:NumericUpDownControl Maximum="{Binding ElementName=PdfViewer, Path=ToplamSayfa}"
                    Minimum="1" SmallChange="1"
                    Value="{Binding SayfaBaşlangıç, UpdateSourceTrigger=PropertyChanged}" />
                <Extensions:NumericUpDownControl Grid.Column="1"
                    Maximum="{Binding ElementName=PdfViewer, Path=ToplamSayfa}"
                    Minimum="1" SmallChange="1"
                    Value="{Binding SayfaBitiş, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="2"
                    Command="{Binding ExtractPdfFile}"
                    CommandParameter="{Binding PdfFilePath, ElementName=PdfViewer, Mode=OneWay}">
                    <Image Width="16" Height="16">
                        <Image.Source>
                            <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                                <Binding Source="Shell32.dll" />
                                <Binding Source="68" />
                            </MultiBinding>
                        </Image.Source>
                    </Image>
                </Button>
                <Button Grid.Column="3"
                    Command="{Binding RemoveSelectedPage}"
                    CommandParameter="{Binding ElementName=PdfViewer}">
                    <Image Width="16" Height="16">
                        <Image.Source>
                            <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                                <Binding Source="Shell32.dll" />
                                <Binding Source="131" />
                            </MultiBinding>
                        </Image.Source>
                    </Image>
                </Button>
                <Button Grid.Column="4"
                    Command="{Binding ArrangePdfFile}"
                    CommandParameter="{Binding ElementName=PdfViewer}">
                    <Image Width="16" Height="16">
                        <Image.Source>
                            <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                                <Binding Source="Imageres.dll" />
                                <Binding Source="273" />
                            </MultiBinding>
                        </Image.Source>
                    </Image>
                </Button>
                <Button Grid.Column="5"
                    BorderBrush="Cyan"
                    Command="{Binding RotateSelectedPage}"
                    CommandParameter="{Binding ElementName=PdfViewer}">
                    <Image Width="16" Height="16"
                        Source="/TwainControl;component/Icons/rotate.png" />
                </Button>
                <Button Grid.Column="6"
                    BorderBrush="Cyan"
                    Command="{Binding AddAllFileToControlPanel}">
                    <Button.CommandParameter>
                        <MultiBinding Converter="{StaticResource CloneConverter}">
                            <Binding Mode="OneWay" RelativeSource="{RelativeSource AncestorType=local:TwainCtrl}" />
                            <Binding ElementName="PdfViewer" Mode="OneWay" />
                        </MultiBinding>
                    </Button.CommandParameter>
                    <Button.Content>
                        <TextBlock FontFamily="Wingdings" FontSize="16" Text="ç" />
                    </Button.Content>
                </Button>
                <Button Grid.Column="7"
                    VerticalContentAlignment="Stretch"
                    Command="{Binding OcrPdfImportViewerThumbnailPage}"
                    CommandParameter="{Binding ElementName=PdfViewer, Mode=OneWay}">
                    <Button.Content>
                        <Grid>
                            <ProgressBar IsIndeterminate="{Binding OcrIsBusy}" />
                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
                                Text="OCR" />
                        </Grid>
                    </Button.Content>
                </Button>
                <Button Grid.Column="8"
                    Command="{Binding ReadPdfTag}"
                    CommandParameter="{Binding ElementName=PdfViewer, Path=PdfFilePath, Mode=OneWay}">
                    <Button.Content>
                        <TextBlock FontFamily="Wingdings 2" FontSize="16" Text="-" />
                    </Button.Content>
                </Button>
                <Button Grid.Column="9" Command="{Binding MergeSelectedImagesToPdfFile}">
                    <Button.CommandParameter>
                        <MultiBinding Converter="{StaticResource CloneConverter}">
                            <Binding Mode="OneWay" RelativeSource="{RelativeSource AncestorType=local:TwainCtrl}" />
                            <Binding ElementName="PdfViewer" Mode="OneWay" />
                        </MultiBinding>
                    </Button.CommandParameter>
                    <Button.Content>
                        <Image Width="16" Height="16">
                            <Image.Source>
                                <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                                    <Binding Source="Imageres.dll" />
                                    <Binding Source="166" />
                                </MultiBinding>
                            </Image.Source>
                        </Image>
                    </Button.Content>
                </Button>
            </Grid>
            <Extensions:NumericUpDownControl IsReadOnly="True"
                Maximum="{Binding ElementName=PdfViewer, Path=ToplamSayfa}"
                Minimum="0" SmallChange="1"
                Value="{Binding PdfSplitCount}" />
            <Button Command="{Binding SplitPdf}" CommandParameter="{Binding ElementName=PdfViewer}">
                <Image Width="16" Height="16">
                    <Image.Source>
                        <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                            <Binding Source="Shell32.dll" />
                            <Binding Source="96" />
                        </MultiBinding>
                    </Image.Source>
                </Image>
            </Button>
            <Button Command="{Binding MaximizePdfControl}">
                <Image Width="16" Height="16">
                    <Image.Source>
                        <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                            <Binding Source="Shell32.dll" />
                            <Binding Source="15" />
                        </MultiBinding>
                    </Image.Source>
                </Image>
            </Button>
        </StackPanel>
        <Grid Grid.Row="1">
            <Pdfviewer:PdfViewer x:Name="PdfViewer"
                AutoFitContent="True" OpenButtonVisibility="Visible"
                PreviewMouseDown="PdfImportViewerControl_MouseDown"
                PreviewMouseMove="PdfImportViewerControl_MouseMove" PrintButtonVisibility="Visible" />
            <Canvas x:Name="cnv" />
        </Grid>
    </Grid>
</UserControl>