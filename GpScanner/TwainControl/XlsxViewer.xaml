<UserControl x:Class="TwainControl.XlsxViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Converter="clr-namespace:TwainControl.Converter"
    xmlns:Extensions="clr-namespace:Extensions;assembly=Extensions"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:twaincontrol="clr-namespace:TwainControl"
    xmlns:twainproperties="clr-namespace:TwainControl.Properties;assembly=TwainControl"
    d:DataContext="{d:DesignInstance Type=twaincontrol:XlsxViewer}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    twaincontrol:Translation.ResourceManager="{x:Static twainproperties:Resources.ResourceManager}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Converter:RandomColorBrushConverter x:Key="RandomColorBrushConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ListBox x:Name="LbSayfalar"
                Grid.Row="0"
                d:ItemsSource="{d:SampleData}"
                ItemsSource="{Binding Tablolar}"
                SelectedItem="{Binding SelectedTable}">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="FontWeight" Value="Bold" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="FontWeight" Value="Bold" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                    <TextBlock Margin="1"
                                        Padding="2"
                                        d:Text="TableName"
                                        Foreground="White"
                                        Text="{Binding TableName}">
                                        <TextBlock.Background>
                                            <Binding Converter="{StaticResource RandomColorBrushConverter}">
                                                <Binding.ConverterParameter>
                                                    <x:Array Type="{x:Type Color}">
                                                        <Color>White</Color>
                                                    </x:Array>
                                                </Binding.ConverterParameter>
                                            </Binding>
                                        </TextBlock.Background>
                                    </TextBlock>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel IsItemsHost="True" Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
            <Extensions:ButtonedTextBox Grid.Row="1"
                IsEnabled="{Binding SelectedTable, Mode=OneWay, Converter={StaticResource NullableToBooleanConverter}}"
                OpenButtonVisibility="Collapsed"
                Text="{Binding Search, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                Watermark="{twaincontrol:Loc TEXT}" />
            <DataGrid Grid.Row="2"
                d:ItemsSource="{d:SampleData}"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                EnableColumnVirtualization="True"
                IsReadOnly="True"
                ItemsSource="{Binding SelectedTable}"
                VirtualizingPanel.CacheLengthUnit="Pixel"
                VirtualizingPanel.IsVirtualizingWhenGrouping="True"
                VirtualizingPanel.ScrollUnit="Pixel"
                VirtualizingPanel.VirtualizationMode="Recycling" />
            <ProgressBar Grid.Row="3"
                Height="3"
                Value="{Binding Progress, Mode=OneWay}" />
        </Grid>
    </Grid>
</UserControl>