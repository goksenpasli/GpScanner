<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Extensions">
    <DataTemplate x:Key="DefaultItemTemplate">
        <CheckBox VerticalAlignment="Center"
            VerticalContentAlignment="Center"
            IsChecked="{Binding IsChecked, Mode=TwoWay}"
            IsEnabled="{Binding IsEnabled, Mode=TwoWay}">
            <StackPanel Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center" Text="{Binding Name, Mode=OneWay}" />
                <ContentPresenter VerticalAlignment="Center" Content="{Binding Content}" />
            </StackPanel>
        </CheckBox>
    </DataTemplate>
    <Style TargetType="{x:Type local:CheckComboBox}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CheckComboBox}">
                    <Grid>
                        <ToggleButton ClickMode="Press"
                            Content="{TemplateBinding SelectionBoxItem}"
                            Focusable="False"
                            IsChecked="{Binding Path=IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" />
                        <TextBlock Margin="2,0,2,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Background="{x:Null}"
                            Focusable="False"
                            IsHitTestVisible="False"
                            Text="{TemplateBinding Text}" />
                        <Popup AllowsTransparency="True"
                            Focusable="False"
                            IsOpen="{TemplateBinding IsDropDownOpen}"
                            Placement="Bottom"
                            PopupAnimation="Slide">
                            <Border MinWidth="{TemplateBinding ActualWidth}"
                                MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
                                BorderBrush="{DynamicResource {x:Static SystemColors.MenuHighlightBrushKey}}"
                                BorderThickness="1"
                                SnapsToDevicePixels="True">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <UniformGrid Grid.Row="0"
                                        Columns="3"
                                        Rows="1">
                                        <Button Command="{Binding SelectAllCommand, RelativeSource={RelativeSource TemplatedParent}}">
                                            <Image Width="32"
                                                Height="32"
                                                Source="/Extensions;component/Icons/selectall.png" />
                                        </Button>
                                        <Button Command="{Binding SelectInverseCommand, RelativeSource={RelativeSource TemplatedParent}}">
                                            <Image Width="32"
                                                Height="32"
                                                Source="/Extensions;component/Icons/inverse.png" />
                                        </Button>
                                        <Button Command="{Binding ClearAllCommand, RelativeSource={RelativeSource TemplatedParent}}">
                                            <Image Width="32"
                                                Height="32"
                                                Source="/Extensions;component/Icons/none.png" />
                                        </Button>
                                    </UniformGrid>
                                    <ItemsControl Grid.Row="1"
                                        ItemTemplate="{Binding ItemTemplate, RelativeSource={RelativeSource TemplatedParent}, FallbackValue={StaticResource DefaultItemTemplate}, TargetNullValue={StaticResource DefaultItemTemplate}}"
                                        ItemsSource="{Binding Path=Items, RelativeSource={RelativeSource TemplatedParent}}"
                                        ScrollViewer.CanContentScroll="True">
                                        <ItemsControl.Template>
                                            <ControlTemplate TargetType="ItemsControl">
                                                <Border Padding="{TemplateBinding Control.Padding}"
                                                    Background="{TemplateBinding Panel.Background}"
                                                    BorderBrush="{TemplateBinding Border.BorderBrush}"
                                                    BorderThickness="{TemplateBinding Border.BorderThickness}"
                                                    SnapsToDevicePixels="True">
                                                    <ScrollViewer Padding="{TemplateBinding Control.Padding}"
                                                        Focusable="False"
                                                        VerticalScrollBarVisibility="Auto">
                                                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                                                    </ScrollViewer>
                                                </Border>
                                            </ControlTemplate>
                                        </ItemsControl.Template>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <VirtualizingStackPanel />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </Grid>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>