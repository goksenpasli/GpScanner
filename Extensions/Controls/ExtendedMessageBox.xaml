<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Extensions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <local:SystemFileIconConverter x:Key="SystemFileIconConverter" />
    <Style TargetType="{x:Type local:ExtendedMessageBox}">
        <Setter Property="Background" Value="{DynamicResource BorderBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource GroupBoxBackGround}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ExtendedMessageBox}">
                    <Border Padding="1"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <ContentPresenter Width="{TemplateBinding CustomContentWidth}"
                                    Height="{TemplateBinding CustomContentHeight}"
                                    Margin="10"
                                    d:Content="Object"
                                    d:Visibility="Visible"
                                    Content="{TemplateBinding CustomContent}"
                                    Visibility="{TemplateBinding CustomContentVisible}" />
                                <TextBlock Margin="0,0,5,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    TextAlignment="Center">
                                    <Run d:Text="TITLE"
                                        FontSize="18"
                                        Text="{TemplateBinding Title}" />
                                    <LineBreak />
                                    <Run d:Text="Message"
                                        FontSize="12"
                                        Text="{TemplateBinding Message}" />
                                </TextBlock>
                            </StackPanel>
                            <CheckBox Margin="5"
                                d:Content="Checkbox"
                                d:Visibility="Visible"
                                Content="{TemplateBinding CheckDescription}"
                                IsChecked="{TemplateBinding IsChecked}"
                                Visibility="{TemplateBinding CheckVisibility}" />
                            <ProgressBar Height="16"
                                Margin="5"
                                d:Value="40"
                                d:Visibility="Visible"
                                IsIndeterminate="{TemplateBinding IsIndeterminate}"
                                Visibility="{TemplateBinding ProgressBarVisibility}"
                                Value="{TemplateBinding ProgressValue}" />
                            <Expander MaxWidth="300"
                                Margin="5"
                                d:Header="HiddenCaption"
                                d:IsExpanded="True"
                                d:Visibility="Visible"
                                Header="{TemplateBinding HiddenCaption}"
                                IsExpanded="{TemplateBinding HiddenDescriptionExpanded}"
                                Visibility="{TemplateBinding HiddenDescriptionVisibility}">
                                <TextBlock d:Text="HiddenDescription"
                                    Text="{TemplateBinding HiddenDescription}"
                                    TextWrapping="Wrap" />
                            </Expander>
                            <Grid HorizontalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <Button x:Name="Yes"
                                    Margin="5"
                                    d:Visibility="Visible"
                                    Background="{x:Null}"
                                    BorderBrush="{x:Null}"
                                    IsDefault="True"
                                    IsEnabled="{TemplateBinding YesEnabled}"
                                    Visibility="{TemplateBinding YesButton}">
                                    <Button.Content>
                                        <Image Width="32" Height="32">
                                            <Image.Style>
                                                <Style TargetType="Image">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding YesIconType, RelativeSource={RelativeSource AncestorType=local:ExtendedMessageBox}}" Value="{x:Static local:IconType.Default}">
                                                            <Setter Property="Source">
                                                                <Setter.Value>
                                                                    <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                                                                        <Binding Source="Comres.dll" />
                                                                        <Binding Source="8" />
                                                                    </MultiBinding>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding YesIconType, RelativeSource={RelativeSource AncestorType=local:ExtendedMessageBox}}" Value="{x:Static local:IconType.Warn}">
                                                            <Setter Property="Source">
                                                                <Setter.Value>
                                                                    <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                                                                        <Binding Source="Comres.dll" />
                                                                        <Binding Source="16" />
                                                                    </MultiBinding>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding YesIconType, RelativeSource={RelativeSource AncestorType=local:ExtendedMessageBox}}" Value="{x:Static local:IconType.Error}">
                                                            <Setter Property="Source">
                                                                <Setter.Value>
                                                                    <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                                                                        <Binding Source="Comres.dll" />
                                                                        <Binding Source="10" />
                                                                    </MultiBinding>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding YesIconType, RelativeSource={RelativeSource AncestorType=local:ExtendedMessageBox}}" Value="{x:Static local:IconType.Wait}">
                                                            <Setter Property="Source">
                                                                <Setter.Value>
                                                                    <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                                                                        <Binding Source="Comres.dll" />
                                                                        <Binding Source="7" />
                                                                    </MultiBinding>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Image.Style>

                                        </Image>
                                    </Button.Content>
                                </Button>
                                <Button x:Name="No"
                                    Grid.Column="1"
                                    Margin="5"
                                    d:Visibility="Visible"
                                    Background="{x:Null}"
                                    BorderBrush="{x:Null}"
                                    IsCancel="True"
                                    IsEnabled="{TemplateBinding NoEnabled}"
                                    Visibility="{TemplateBinding NoButton}">
                                    <Button.Content>
                                        <Image Width="32" Height="32">
                                            <Image.Source>
                                                <MultiBinding Converter="{StaticResource SystemFileIconConverter}">
                                                    <Binding Source="Comres.dll" />
                                                    <Binding Source="13" />
                                                </MultiBinding>
                                            </Image.Source>
                                        </Image>
                                    </Button.Content>
                                </Button>
                            </Grid>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>